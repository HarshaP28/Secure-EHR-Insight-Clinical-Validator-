models:
  - type: main
    engine: openai
    model: deepseek-chat
    parameters:
      base_url: "https://api.deepseek.com/v1"

  # FDE FIX: Route embeddings locally using SentenceTransformers
  - type: embeddings
    engine: SentenceTransformers
    model: NeuML/bioclinical-modernbert-base-embeddings
